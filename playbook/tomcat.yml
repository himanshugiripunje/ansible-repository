- name: splitting examples
  hosts: all
  vars: 
    - emails: ["himanshu@gmail.com", "meanshu@gmail.com"]
  tasks: 
    - name: printing only domain names from email addresses with split
      debug: msg="{{ emails | split("@") | front }}"
 
        